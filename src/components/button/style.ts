import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    bPrimary: {
        backgroundColor: 'white',
        borderRadius: 5,
        margin: 10,
    },
    text:{
        color: 'black',
        fontWeight: "bold",
        padding: 10,
        fontSize: 18,
        textAlign: "center"
    }
})